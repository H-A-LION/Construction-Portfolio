# ============================================================================
# BUILDPORT API - INDIVIDUAL TEST COMMANDS
# Fixed version - Copy and paste each command one by one
# ============================================================================

# ============================================================================
# 1. AUTHENTICATION TESTS
# ============================================================================

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "▶ 1. AUTHENTICATION TESTS"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

echo "▶ 1.1 Login (Public) - NO jq, just raw output"
curl -s -X POST \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@buildport.com","password":"admin123"}' \
  http://localhost:8000/api/v1/auth/login

echo ""
echo "▶ 1.2 Get Current User (Protected)"
curl -s -X GET \
  -H "Authorization: Bearer $TOKEN" \
  http://localhost:8000/api/v1/auth/me

echo ""
echo "▶ 1.3 Update Profile (Protected)"
curl -s -X PUT \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"name":"Admin User Updated","email":"admin@buildport.com"}' \
  http://localhost:8000/api/v1/auth/update

echo ""
echo "▶ 1.4 Logout (Protected)"
curl -s -X POST \
  -H "Authorization: Bearer $TOKEN" \
  http://localhost:8000/api/v1/auth/logout

echo ""
echo ""

# ============================================================================
# 2. CONTENT TESTS
# ============================================================================

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "▶ 2. CONTENT TESTS"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

echo "▶ 2.1 Get Hero Content (Public)"
curl -s -X GET \
  http://localhost:8000/api/v1/content/hero

echo ""
echo "▶ 2.2 Get All Content (Protected)"
curl -s -X GET \
  -H "Authorization: Bearer $TOKEN" \
  http://localhost:8000/api/v1/content/all

echo ""
echo "▶ 2.3 Update Hero Content (Protected)"
curl -s -X PUT \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"data":{"badge":"🏗️ Premier Construction 2024","title":"Building <span>Tomorrow</span> Today","description":"We transform visions into reality","stats":[{"number":"450","label":"Projects"},{"number":"98","label":"Satisfaction"},{"number":"25","label":"Awards"}]}}' \
  http://localhost:8000/api/v1/content/hero

echo ""
echo "▶ 2.4 Get Content History (Protected)"
curl -s -X GET \
  -H "Authorization: Bearer $TOKEN" \
  http://localhost:8000/api/v1/content/hero/history

echo ""
echo "▶ 2.5 Revert to Version 1 (Protected)"
curl -s -X POST \
  -H "Authorization: Bearer $TOKEN" \
  http://localhost:8000/api/v1/content/hero/revert/1

echo ""
echo ""

# ============================================================================
# 3. PROJECT TESTS (CRUD)
# ============================================================================

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "▶ 3. PROJECT TESTS"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

echo "▶ 3.1 Get All Projects (Protected)"
curl -s -X GET \
  -H "Authorization: Bearer $TOKEN" \
  http://localhost:8000/api/v1/projects

echo ""
echo "▶ 3.2 Create Project (Protected)"
curl -s -X POST \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"title":"Modern Office Tower","description":"A 50-story commercial office tower","category":"Commercial","location":"New York City"}' \
  http://localhost:8000/api/v1/projects

echo ""
echo "▶ 3.3 Get Single Project (Replace 1 with actual ID from create response)"
curl -s -X GET \
  -H "Authorization: Bearer $TOKEN" \
  http://localhost:8000/api/v1/projects/2

echo ""
echo "▶ 3.4 Update Project (Replace 2 with actual ID)"
curl -s -X PUT \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"title":"Updated Office Tower"}' \
  http://localhost:8000/api/v1/projects/2

echo ""
echo "▶ 3.5 Delete Project (Replace 1 with actual ID)"
curl -s -X DELETE \
  -H "Authorization: Bearer $TOKEN" \
  http://localhost:8000/api/v1/projects/2

echo ""
echo "▶ 3.6 Reorder Projects"
curl -s -X PUT \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"orders":[{"id":1,"order":0},{"id":2,"order":1},{"id":3,"order":2}]}' \
  http://localhost:8000/api/v1/projects/reorder

echo ""
echo ""

# ============================================================================
# 4. SERVICE TESTS (CRUD)
# ============================================================================

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "▶ 4. SERVICE TESTS"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

echo "▶ 4.1 Get All Services (Protected)"
curl -s -X GET \
  -H "Authorization: Bearer $TOKEN" \
  http://localhost:8000/api/v1/services

echo ""
echo "▶ 4.2 Create Service (Protected)"
curl -s -X POST \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"title":"Architectural Design","description":"Full architectural design services","icon":"fa-building"}' \
  http://localhost:8000/api/v1/services

echo ""
echo "▶ 4.3 Get Single Service (Replace 2 with actual ID from create response)"
curl -s -X GET \
  -H "Authorization: Bearer $TOKEN" \
  http://localhost:8000/api/v1/services/2

echo ""
echo "▶ 4.4 Update Service (Replace 1 with actual ID)"
curl -s -X PUT \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"title":"Updated Architectural Design"}' \
  http://localhost:8000/api/v1/services/2

echo ""
echo "▶ 4.5 Delete Service (Replace 2 with actual ID)"
curl -s -X DELETE \
  -H "Authorization: Bearer $TOKEN" \
  http://localhost:8000/api/v1/services/2

echo ""
echo "▶ 4.6 Reorder Services"
curl -s -X PUT \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"orders":[{"id":2,"order":0},{"id":3,"order":1},{"id":4,"order":3}]}' \
  http://localhost:8000/api/v1/services/reorder

echo ""
echo ""

# ============================================================================
# 5. TEAM TESTS (CRUD)
# ============================================================================

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "▶ 5. TEAM TESTS"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

echo "▶ 5.1 Get All Team Members (Protected)"
curl -s -X GET \
  -H "Authorization: Bearer $TOKEN" \
  http://localhost:8000/api/v1/team

echo ""
echo "▶ 5.2 Create Team Member (Protected)"
curl -s -X POST \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"name":"John Architect","role":"Lead Architect","bio":"20 years experience in commercial architecture","experience":"20 years"}' \
  http://localhost:8000/api/v1/team

echo ""
echo "▶ 5.3 Get Single Team Member (Replace 1 with actual ID from create response)"
curl -s -X GET \
  -H "Authorization: Bearer $TOKEN" \
  http://localhost:8000/api/v1/team/2

echo ""
echo "▶ 5.4 Update Team Member (Replace 2 with actual ID)"
curl -s -X PUT \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"name":"John Architect Sr.","role":"Senior Lead Architect"}' \
  http://localhost:8000/api/v1/team/2

echo ""
echo "▶ 5.5 Delete Team Member (Replace 1 with actual ID)"
curl -s -X DELETE \
  -H "Authorization: Bearer $TOKEN" \
  http://localhost:8000/api/v1/team/2

echo ""
echo "▶ 5.6 Reorder Team Members"
curl -s -X PUT \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"orders":[{"id":2,"order":0},{"id":3,"order":1},{"id":4,"order":2}]}' \
  http://localhost:8000/api/v1/team/reorder

echo ""
echo ""

# ============================================================================
# 6. IMAGE UPLOAD TESTS
# ============================================================================

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "▶ 6. IMAGE UPLOAD TESTS"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

echo "▶ 6.1 Upload Hero Image (Replace path with actual image)"
curl -s -X POST \
  -H "Authorization: Bearer $TOKEN" \
  -F "hero_image=@/home/hkali/Desktop/VioletPro/backend/uploads/Home.jpg" \
  http://localhost:8000/api/v1/content/hero/upload-image

echo ""
echo "▶ 6.2 Delete Hero Image"
curl -s -X DELETE \
  -H "Authorization: Bearer $TOKEN" \
  http://localhost:8000/api/v1/content/hero/delete-image

echo ""
echo ""

# ============================================================================
# 7. BULK CREATE (Create multiple records at once)
# ============================================================================

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "▶ 7. BULK CREATE TESTS"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

echo "▶ 7.1 Create 3 Projects in a loop"
for i in 1 2 3; do
  echo "Creating Project $i..."
  curl -s -X POST \
    -H "Content-Type: application/json" \
    -H "Authorization: Bearer $TOKEN" \
    -d "{\"title\":\"Test Project $i\",\"description\":\"Test project $i description\",\"category\":\"Test\",\"location\":\"Test Location\"}" \
    http://localhost:8000/api/v1/projects
  echo ""
done

echo "▶ 7.2 Create 3 Services in a loop"
for i in 1 2 3; do
  echo "Creating Service $i..."
  curl -s -X POST \
    -H "Content-Type: application/json" \
    -H "Authorization: Bearer $TOKEN" \
    -d "{\"title\":\"Test Service $i\",\"description\":\"Test service $i description\",\"icon\":\"fa-test-$i\"}" \
    http://localhost:8000/api/v1/services
  echo ""
done

echo "▶ 7.3 Create 3 Team Members in a loop"
for i in 1 2 3; do
  echo "Creating Team Member $i..."
  curl -s -X POST \
    -H "Content-Type: application/json" \
    -H "Authorization: Bearer $TOKEN" \
    -d "{\"name\":\"Test Member $i\",\"role\":\"Test Role $i\",\"bio\":\"Test member $i bio\",\"experience\":\"5 years\"}" \
    http://localhost:8000/api/v1/team
  echo ""
done

echo ""
echo ""

# ============================================================================
# 8. ERROR TESTING (These should fail)
# ============================================================================

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "▶ 8. ERROR TESTING (These should fail)"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

echo "▶ 8.1 Wrong Password (Should return 401)"
curl -s -X POST \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@buildport.com","password":"wrongpassword"}' \
  http://localhost:8000/api/v1/auth/login

echo ""
echo "▶ 8.2 Non-existent Email (Should return 401)"
curl -s -X POST \
  -H "Content-Type: application/json" \
  -d '{"email":"nonexistent@buildport.com","password":"admin123"}' \
  http://localhost:8000/api/v1/auth/login

echo ""
echo "▶ 8.3 Invalid Token (Should return 401)"
curl -s -X GET \
  -H "Authorization: Bearer invalid_token_12345" \
  http://localhost:8000/api/v1/auth/me

echo ""
echo "▶ 8.4 No Token (Should return 401)"
curl -s -X GET \
  http://localhost:8000/api/v1/auth/me

echo ""
echo "▶ 8.5 Get Non-existent Project (Should return 404)"
curl -s -X GET \
  -H "Authorization: Bearer $TOKEN" \
  http://localhost:8000/api/v1/projects/99999

echo ""
echo ""

# ============================================================================
# 9. CLEANUP - Delete Test Records
# ============================================================================

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "▶ 9. CLEANUP - Delete Test Records"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

echo "▶ 9.1 Delete all test projects"
curl -s -X GET -H "Authorization: Bearer $TOKEN" http://localhost:8000/api/v1/projects | \
  grep -o '"id":[0-9]*' | cut -d: -f2 | \
  while read id; do
    # Check if it's a test project
    TITLE=$(curl -s -X GET -H "Authorization: Bearer $TOKEN" http://localhost:8000/api/v1/projects/$id | grep -o '"title":"[^"]*"' | head -1)
    if echo "$TITLE" | grep -q "Test"; then
      echo "Deleting test project $id..."
      curl -s -X DELETE -H "Authorization: Bearer $TOKEN" http://localhost:8000/api/v1/projects/$id
      echo ""
    fi
  done

echo "▶ 9.2 Delete all test services"
curl -s -X GET -H "Authorization: Bearer $TOKEN" http://localhost:8000/api/v1/services | \
  grep -o '"id":[0-9]*' | cut -d: -f2 | \
  while read id; do
    TITLE=$(curl -s -X GET -H "Authorization: Bearer $TOKEN" http://localhost:8000/api/v1/services/$id | grep -o '"title":"[^"]*"' | head -1)
    if echo "$TITLE" | grep -q "Test"; then
      echo "Deleting test service $id..."
      curl -s -X DELETE -H "Authorization: Bearer $TOKEN" http://localhost:8000/api/v1/services/$id
      echo ""
    fi
  done

echo "▶ 9.3 Delete all test team members"
curl -s -X GET -H "Authorization: Bearer $TOKEN" http://localhost:8000/api/v1/team | \
  grep -o '"id":[0-9]*' | cut -d: -f2 | \
  while read id; do
    NAME=$(curl -s -X GET -H "Authorization: Bearer $TOKEN" http://localhost:8000/api/v1/team/$id | grep -o '"name":"[^"]*"' | head -1)
    if echo "$NAME" | grep -q "Test"; then
      echo "Deleting test team member $id..."
      curl -s -X DELETE -H "Authorization: Bearer $TOKEN" http://localhost:8000/api/v1/team/$id
      echo ""
    fi
  done

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "✅ TEST COMPLETE"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

# ============================================================================
# END OF TESTS
# ============================================================================